// Command palette, diagnostics advice, and diagnostic toggles.
export global DiagnosticsActions {
    callback execute_advice(string);
    callback toggle_cloner(int);
    callback toggle_dim();
    callback toggle_talkback();
    callback toggle_monitor_output();
}
