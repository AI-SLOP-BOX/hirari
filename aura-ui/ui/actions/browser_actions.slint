// Sample library and browser interaction contract.
export global BrowserActions {
    callback browser_search(string);
    callback browser_scan(string);
    callback browser_preview(string);
    callback import_sample(string, int, float);
}
