export global StackActions {
    callback create_from_selected(int);
    callback add_selected(int);
    callback delete_stack();
    callback gain_changed(float);
    callback collapsed_changed(bool);
}
