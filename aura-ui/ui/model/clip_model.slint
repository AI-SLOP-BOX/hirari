export struct Z_Clip {
    id: int,
    name: string,
    start_beat: float,
    length_beats: float,
    color: color,
    points: [float],
    fade_in: float,
    fade_out: float,
    gain: float,
    reverse: bool,
    warp_ratio: float,
    pitch_semitones: float,
    loop_count: int,
    trim_start: float,
    trim_end: float,
    layer: int,
    selected: bool,
    missing: bool,
}
