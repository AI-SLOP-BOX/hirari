# Aura DAW プレビュー版リリースチェックリスト

## リリース判定

プレビュー版は、機能の多さよりも次の状態を満たすことを優先する。

> 起動できる、音が鳴る、基本操作ができる、保存・復元できる、異常入力で落ちない。

## 最新実装ステータス（2026-08-10）

| 確認対象 | 現状 | 判定 |
| --- | --- | --- |
| Missing音源 | Missing判定、再リンクAPI、`RELINK MISSING`操作、一意なファイル名による自動復旧を実装 | 実装済み／実機確認待ち |
| Save As | Save Asで指定したプロジェクトパスへ保存し、同一セッション内の次回Saveでも保持 | 実装済み／実機確認待ち |
| ショートカット | Space、`⌘/Ctrl+O`、`⌘/Ctrl+S`、`⌘/Ctrl+I`、`⌘⇧I / Ctrl+Shift+I`、`⌘/Ctrl+Z`、`⌘/Ctrl+Y`を実装 | 実装済み／実機確認待ち |
| 実ビルド | `cargo check/test/build --workspace` が成功、61テスト全件パス | 成功／実機起動確認待ち |
| デバイス再接続 | オフライン時に約1秒間隔で再接続を試行。復旧後はユーザー操作で再生可能 | コード確認済み／実機確認待ち |
| SR表示 | エンジンのサンプルレートを`SR: N Hz`（未取得時は`SR: --`）としてUI表示 | コード確認済み／実機確認待ち |
| PDC | 最大ノード遅延を基準に補正オフセットを算出し、回帰テストを追加 | 実装済み／統合確認待ち |
| グリッド／音楽時計 | BPM・サンプルレートからスナップ位置、小節、ビートを決定論的に算出 | 実装済み／統合確認待ち |
| テイク複製 | プレイリストを複製し、新しいオルタナティブをアクティブ化 | 実装済み／UI確認待ち |
| プレビュー音源キャッシュ | WAVの再帰スキャン、PCM16/PCM32デコード、登録・プリロード、16パッド割り当てAPIを実装。インポート時の登録もUIから接続 | 実装済み／実音声グラフ接続待ち |

プレビュー判定で残る必須作業は、macOS実機での起動、音声入出力、Save As・再リンク・ショートカット、デバイス切断／再接続、長時間再生の確認に限定する。プレビュー音源キャッシュはコアAPIとUIインポートまで実装済みだが、ドラムパッドのノートイベントから実サンプルを鳴らす実音声グラフ接続は次の作業として残る。

## P0：プレビュー版に必須

### 1. 起動とオーディオデバイス

- [ ] アプリが正常に起動する
- [ ] 初期プロジェクトが表示される
- [x] CoreAudioデバイスを初期化できる（実デバイスのSR／バッファサイズを取得して使用）
- [ ] オーディオデバイスが存在しない場合もクラッシュしない
- [x] デバイス切断時は自動的に再生をPauseする
- [x] UIにデバイス切断状態（`AUDIO: READY / OFFLINE`）を表示する
- [x] デバイス復旧後はユーザー操作で再生を再開できる

### 2. 再生・停止・再生ヘッド

- [ ] 再生、停止、一時停止が動作する
- [ ] 再生ヘッドが実時間で進む
- [ ] UIの再生ヘッドをSlint Timerで約60 FPS更新する
- [ ] 再生ヘッドの表示とオーディオエンジンのサンプル位置が一致する
- [ ] 44.1kHzと48kHzで正常に動作する
- [ ] シーク後に正しい位置から再生できる

### 3. テンポとグリッド

- [ ] BPM変更がオーディオエンジンへ反映される
- [ ] BPMを20〜300の範囲で安全に制限する
- [x] グリッド位置がBPMとサンプルレートに同期する（コア計算実装済み）
- [x] 再生ヘッド、グリッド、リージョン位置の単位変換が一致する（コア計算実装済み）
- [x] NaN、Inf、ゼロサンプルレートを拒否する

### 4. 音声ファイルのインポートと再生

- [x] WAVなどの対応音声ファイルをインポートできる
- [x] インポートした実音声が再生される
- [ ] ファイルパスが存在しない場合に安全に失敗する
- [ ] 壊れたファイルや未対応形式でクラッシュしない
- [ ] 空ファイルを安全に拒否する
- [ ] サンプルレートが異なる音声を安全に扱う

### 5. 実波形表示

- [ ] ダミー・ランダム波形を使用しない
- [ ] 実デコーダの音声データからピークを生成する
- [ ] 波形キャッシュを利用してUIをブロックしない
- [ ] インポート直後にUIへ波形を反映する
- [ ] リージョン削除時に波形キャッシュも削除する
- [ ] 空の波形データでもUIが壊れない

### 6. トラック基本操作

- [ ] トラック追加が動作する
- [ ] トラック削除が動作する
- [ ] トラック名変更が動作する
- [ ] 音量とパンが反映される
- [ ] ミュートとソロが反映される
- [x] UIモデルとC++エンジンのトラック状態が一致する（空状態も同期）
- [x] UI選択行番号と実トラックIDを分離して処理する
- [ ] 削除中のトラックをオーディオスレッドが参照し続けない
- [ ] Use-after-free、範囲外アクセス、二重削除が発生しない

### 7. プロジェクト保存・読み込み

- [x] プロジェクトをSaveできる
- [x] トラック情報をSave/Openで復元できる
- [x] リージョンの位置、長さ、ミュート状態をSave/Openで復元できる
- [x] BPMとサンプルレートをSave/Openで復元できる
- [x] 音声ファイルパスをSaveできる
- [x] Save Asで指定したパスへプロジェクトを保存できる
- [x] 保存成功後、同一セッション内の次回SaveでSave pathを保持する
- [ ] アプリ再起動後も前回のSave pathを保持する
- [x] Open後にUIトラックモデルを再構築する
- [x] Open後にリージョンと波形を再表示する（波形生成完了後はUIポーリングで更新）
- [x] 壊れたプロジェクトを安全に拒否する
- [x] 一時ファイル経由のアトミック保存を使用する（Rust保存経路で実装・テスト済み）

### 8. リアルタイムオーディオ安全性

- [ ] オーディオコールバック内でファイルI/Oを行わない
- [ ] オーディオコールバック内で通常のMutex待機を行わない
- [ ] 再生中の大規模メモリアロケーションを避ける
- [ ] DSP処理でNaNとInfを出力しない
- [ ] 不正なMIDIイベントでクラッシュしない
- [ ] バッファサイズ0や異常なサンプルレートを処理できる
- [ ] フィルター係数をNyquist周波数内に制限する
- [ ] オーディオスレッドのPanicや例外を発生させない

## P1：プレビュー後でもよい機能

- [ ] VST3 / AU / CLAPプラグインホスティング
- [ ] プラグインサンドボックス
- [ ] 本格的なMIDIエディター
- [ ] オートメーション編集
- [ ] タイムストレッチとワープ
- [ ] 完全なUndo/Redo履歴
- [ ] バス、サイドチェイン、複雑なルーティング
- [ ] 高品質なオフラインバウンス
- [ ] 高度なミキサー機能
- [ ] 自動保存とクラッシュリカバリー

## 採用する設計判断

### 再生ヘッド同期

プレビュー版では、UIスレッドの `slint::Timer` からエンジン状態をポーリングする方式を採用する。

理由：

- 実装とデバッグが単純
- UI更新と同じスレッドで安全に処理できる
- 約60 FPSの表示には十分
- バックグラウンドスレッドからUIへイベントを送る複雑さを避けられる

オーディオ処理そのものはCoreAudioコールバック側で継続し、Slint Timerは表示更新だけを担当する。

### オーディオデバイス切断

デバイス切断時は自動Pauseを採用する。

期待する挙動：

1. デバイス切断を検出する
2. 再生を停止またはPauseする
3. UIに切断状態を表示する
4. デバイス復旧後、ユーザー操作で再生を再開する

これにより、無音状態での再生継続、異常出力、再接続時の状態不整合を防止する。

## 手動スモークテスト

### 起動

- [ ] オーディオデバイス接続状態で起動する
- [ ] デバイスなしで起動する
- [ ] 初期トラックが表示される

### 再生

- [ ] 再生・停止・Pauseを複数回行う
- [ ] 再生中にUIを操作する
- [ ] 再生中にシークする
- [ ] BPMを変更する

### 編集

- [ ] トラックを追加・削除する
- [ ] 音声ファイルをインポートする
- [ ] 波形が実音声に対応していることを確認する
- [ ] リージョンを移動する

### 保存と復元

- [ ] プロジェクトを保存する
- [ ] アプリを再起動する
- [ ] プロジェクトをOpenする
- [ ] トラック、リージョン、BPM、波形が復元されることを確認する

### 異常系

- [ ] 存在しない音声ファイルをインポートする
- [ ] 壊れたプロジェクトをOpenする
- [ ] オーディオデバイスを切断する
- [ ] 空ファイルを読み込む
- [ ] 極端なBPM・サンプルレートを入力する

## リリースゲート

次の条件をすべて満たした場合にプレビュー版を配布可能とする。

- [ ] P0チェック項目がすべて完了
- [ ] 手動スモークテストが完了
- [ ] `cargo fmt --all -- --check` が成功
- [ ] `cargo check --workspace` が成功
- [ ] `cargo test --workspace` が成功
- [x] ワークスペーステスト61件が全件パス
- [ ] 30分以上の連続再生でクラッシュ・音切れがない
- [ ] プロジェクト保存・読み込みを複数回行って状態が壊れない
- [ ] オーディオデバイス切断時にクラッシュしない

## 安定性・障害復旧

- [ ] 起動時に前回の異常終了を検出できる
- [ ] 壊れた一時ファイルを自動的に無視できる
- [ ] 保存中にアプリが終了しても元のプロジェクトが壊れない
- [ ] 読み込み失敗時に現在のプロジェクトを破壊しない
- [ ] 不正なトラックID・リージョンIDを安全に拒否する
- [ ] 同じファイルを複数回インポートしてもキャッシュが衝突しない
- [ ] トラック削除後に再生を継続してもクラッシュしない
- [ ] オーディオデバイスの再接続を複数回繰り返しても状態が壊れない
- [ ] エラー発生時にユーザーが再試行できる

## 性能・リアルタイム要件

- [ ] オーディオコールバックの最大処理時間を計測する
- [ ] 44.1kHz / 48kHz、128 / 256 / 512 / 1024 samplesで確認する
- [ ] 再生中のUI操作で音切れが発生しない
- [ ] 100トラック程度で再生ヘッドが滑らかに更新される
- [ ] 大きな音声ファイルの波形生成をバックグラウンド処理する
- [ ] 波形生成中も再生とUI操作を継続できる
- [ ] キャッシュヒット時の波形表示が即時に行われる
- [ ] UIスレッドで同期的なファイル読み込みを行わない
- [ ] プロジェクト読み込み中に進捗または処理中表示を出す
- [ ] メモリ使用量が再生時間に比例して増加し続けない
- [ ] 30分以上の連続再生後もメモリが安定する

## 音声フォーマット互換性

- [ ] WAV PCM 16-bitを読み込める
- [ ] WAV PCM 24-bitを読み込める
- [ ] WAV PCM 32-bit floatを読み込める
- [ ] モノラル音源を正しく扱える
- [ ] ステレオ音源を正しく扱える
- [ ] 44.1kHz、48kHz、96kHz音源を扱える
- [ ] 異なるサンプルレートの音源を正しく再生できる
- [ ] 非対応形式を明確なエラーとして表示する
- [ ] Unicodeを含むファイルパスを扱える
- [ ] 外付けドライブ上の音源を扱える
- [ ] ファイルが移動・削除された場合にMissing表示を出す
- [x] 存在しない音源をコア側でMissingとして判定する
- [x] 一意なファイル名に一致する既存音源をコア側で自動復旧する

## 編集操作の受け入れ条件

- [ ] クリップをタイムライン上で移動できる
- [ ] クリップの開始位置がサンプル単位で破綻しない
- [ ] クリップの長さが負数にならない
- [ ] クリップをプロジェクト範囲外へ移動した場合も安全に処理する
- [ ] フェードイン・フェードアウトが適用される
- [ ] クリップゲインが音声再生へ反映される
- [ ] ミュート状態が保存・復元される
- [ ] 操作対象トラックが常に明確に表示される
- [ ] 操作をUndo/Redoできる、または未対応であることを明示する
- [ ] 編集中に再生しても音声バッファが破綻しない

## UI・ユーザー導線

- [ ] 起動直後に主要操作（Open、Save、Play、Stop）が確認できる
- [ ] 音声デバイスの状態を表示する
- [x] サンプルレートをUIで確認できる（`SR: N Hz`／未取得時`SR: --`）
- [ ] バッファサイズをUIで確認できる
- [ ] BPMを編集できる
- [x] Space、`⌘/Ctrl+O`、`⌘/Ctrl+S`、`⌘/Ctrl+I`、`⌘⇧I / Ctrl+Shift+I`、`⌘/Ctrl+Z`、`⌘/Ctrl+Y` のショートカットが実装されている
- [ ] 処理中・成功・失敗の状態を画面に表示する
- [ ] ファイルパスが長い場合も省略表示できる
- [x] Missing音源を再リンクできる（`RELINK MISSING`／`⌘⇧I`・`Ctrl+Shift+I`）
- [ ] キーボード操作だけでも再生・停止できる
- [ ] 高DPI表示で主要ボタンが欠けない
- [ ] ウィンドウサイズ変更後もタイムラインが操作できる
- [ ] UIのダミー表示やAI処理中表示をプレビュー必須画面に残さない

## ログ・診断

- [ ] 起動、デバイス初期化、保存、読み込み、インポートをログに記録する
- [ ] ログにタイムスタンプと重要度を含める
- [ ] オーディオコールバックから標準出力へログを出さない
- [ ] デバイス切断・再接続を記録する
- [ ] デコード失敗のファイルパスと理由を記録する
- [ ] DSPのNaN/Inf発生を検出できる
- [ ] オーディオ処理時間の最大値と平均値を確認できる
- [ ] CPU・メモリ使用量を確認できる
- [ ] クラッシュ時に直前の操作とプロジェクトパスを復元できる
- [ ] 個人情報や機密ファイル内容をログに出さない

## 配布・インストール

- [ ] クリーンなmacOS環境で起動できる
- [ ] 必要なリソースとMetal/CoreAudio依存を同梱する
- [ ] Apple Siliconで起動できる
- [ ] Intel Mac対応方針を明記する
- [ ] アプリの署名・隔離属性を確認する
- [ ] 初回起動時の権限要求を説明する
- [ ] アプリ終了時にオーディオデバイスを解放する
- [ ] 古い設定ファイルがあっても起動できる
- [ ] アンインストール時にユーザープロジェクトを削除しない
- [ ] バージョン番号とビルド日時を確認できる

## テストマトリクス

| 項目 | 最低確認条件 |
| --- | --- |
| OS | 対応macOSの最小バージョン、現行バージョン |
| CPU | Apple Silicon、Intel対応方針に基づく環境 |
| サンプルレート | 44.1kHz、48kHz、96kHz |
| バッファサイズ | 128、256、512、1024 samples |
| チャンネル | モノ、ステレオ |
| 音声形式 | PCM16、PCM24、Float32 WAV |
| プロジェクト | 空、通常、大規模、破損ファイル |
| デバイス | 内蔵出力、外部USB、切断・再接続 |
| 操作 | 再生中の編集、保存、Open、削除、インポート |
| 負荷 | 100トラック、長時間再生、大型音声ファイル |

## 既知の制限を明示する項目

- [ ] 未実装のプラグイン形式を明記する
- [ ] 対応していない音声フォーマットを明記する
- [ ] Undo/Redoの対応範囲を明記する
- [ ] プロジェクト互換性とバージョン方針を明記する
- [ ] 外部音源のパス移動時の再リンク方法を明記する
- [ ] MIDI入力・出力の対応範囲を明記する
- [ ] オフラインバウンスの制限を明記する
- [ ] 既知の音切れ・レイテンシー条件を明記する
- [ ] プレビュー版で収集するクラッシュ・診断情報を明記する

## プレビュー利用者から収集する情報

- [ ] macOSバージョン
- [ ] CPUアーキテクチャ
- [ ] オーディオデバイス名
- [ ] サンプルレートとバッファサイズ
- [ ] プロジェクト規模
- [ ] 使用音声フォーマット
- [ ] 再生時間と発生頻度
- [ ] 音切れ・クラッシュ・波形不一致の再現手順
- [ ] ログの同意取得と匿名化

## 現在の到達状況と残課題

### 実装・コード確認済み

- [x] Rustワークスペースのフォーマット確認
- [x] `cargo build --workspace` 成功
- [x] `cargo check --workspace` 成功
- [x] `cargo test --workspace` 成功
- [x] ワークスペーステスト20件全件パス
- [x] C++主要ヘッダーのC++17構文チェック成功
- [x] PDC・オルタナティブ複製の回帰テストを追加
- [x] グリッド・音楽時計の不正入力ガードを追加
- [x] UIのランダム波形生成を廃止
- [x] 実デコーダ由来の波形ピーク取得経路を追加
- [x] トラック追加・削除時のUI/Core同期
- [x] Open Project後のUIモデル再構築
- [x] BPM変更のUI/Core接続
- [x] プロジェクト不正データの検証
- [x] CoreAudio初期化失敗時のロールバック
- [x] AudioUnit部分初期化時の安全解放
- [x] デバイス切断時のコールバック停止
- [x] 線形パラメータスムーザーのアルゴリズム統一
- [x] 空のC++翻訳単位によるアーカイブ警告の解消

### コード上は対応済みだが実機確認が必要

- [ ] macOSでアプリを実際に起動する
- [ ] 内蔵出力から実音声を確認する
- [ ] USBオーディオデバイスで再生する
- [ ] デバイス切断中に自動Pauseされることを確認する
- [ ] デバイス再接続後に手動再生できることを確認する
- [ ] 44.1kHz / 48kHzで再生する
- [ ] 128 / 256 / 512 samplesで再生する
- [ ] WAVをインポートして実音声を確認する
- [ ] インポート直後に波形が表示されることを確認する
- [ ] Open後にトラック・リージョン・波形が一致することを確認する
- [ ] 再生中にトラック削除してクラッシュしないことを確認する
- [ ] 30分以上連続再生する
- [ ] 大きなプロジェクトを読み込む

### 実機検証に限定した残課題

- [x] OSのファイル選択ダイアログをOpen Projectに接続する
- [x] OSのファイル選択ダイアログを音声インポートに接続する
- [ ] macOS実機で起動し、実音声を再生する
- [ ] Save As → 再起動 → Openでプロジェクトを復元する
- [ ] Missing音源を実機で再リンクし、音声・長さ・波形を確認する
- [ ] ショートカットを実機で確認する
- [ ] デバイス切断・自動Pause・再接続後の手動再生を確認する
- [ ] 44.1kHz / 48kHz、各バッファサイズで再生する
- [ ] 30分以上連続再生し、音切れ・クラッシュがないことを確認する
- [ ] 大きなプロジェクトと100トラック程度の負荷を確認する
- [ ] クリーンなmacOS環境で起動・インストールを確認する

## プレビュー版の最小スコープ

プレビュー版では、次の機能を完成基準とする。

1. WAVを1つインポートできる
2. 実音声を再生・停止できる
3. 実波形がタイムラインに表示される
4. トラックを追加・削除できる
5. BPMと再生ヘッドが同期する
6. プロジェクトを保存できる
7. 保存したプロジェクトをOpenできる
8. オーディオデバイス切断時にクラッシュせずPauseする
9. 不正な音声・プロジェクト入力で既存状態を破壊しない
10. 上記をmacOS実機で再現確認できる

## 実装を後回しにする機能

以下は、最小スコープが安定するまでプレビュー版の必須条件に含めない。

- [ ] VST3 / AU / CLAPの完全なプラグインホスティング
- [ ] プラグインサンドボックス
- [ ] AIによる自動伴奏・自動ミックス
- [ ] 高度なタイムストレッチ
- [ ] 完全なMIDI編集
- [ ] 高度なサイドチェイン・ルーティング
- [ ] 完全なオフラインバウンス
- [ ] 高度な動画・スペクトル機能

## 最終リリース判定

### 最新ビルド確認（2026-08-10）

- `cargo fmt --all`：成功
- `cargo check --workspace`：成功
- `cargo test --workspace`：20件中20件成功
- `cargo build --workspace`：成功
- C++17主要ヘッダー構文チェック：成功
- C++20主要ソース構文チェック：成功（engine_clock / MIDI / mastering / FFI / DSP / CoreAudio）
- `AuraUltimate.hpp` C++20構文チェック：成功
- C++旧統合層のTimeline／AudioBuffer／MIDI API互換修正：完了
- 開発用`aura-ui`実機起動：画像デコードエラーと空ピーク配列panicを修正後、プロセス継続を確認
- Metal compute pipeline：欠落function時の安全なフォールバックガードを追加
- `TEST TONE`：コマンドパレットから440Hz検証音を出力する経路を追加
- CMake全体ビルド：`CMakeLists.txt`が削除状態のため未実行

次の実機確認が未完了の場合、実ビルドが成功していてもプレビュー版としては未完成とする。

- 実機で音が鳴ることの未確認
- デバイス切断・再接続の未確認
- 30分連続再生の未確認
- Save As後の再起動・復元の未確認
- Missing音源の再リンク未確認
- ショートカット未確認

## プレビュー版までの見込み

### 現在地

コード上のP0機能は大部分が接続済みです。

- Open / Save / Import：基本経路実装済み／復元統合と実機確認待ち
- 実波形表示：デコーダとピーク生成は実装済み／非同期更新とキャッシュ無効化が未完了
- トラック追加・削除：実装済み
- BPM・再生ヘッド同期：実装済み
- CoreAudio初期化・切断・再接続：実装済み
- 自動Pause：実装済み

詳細な未実装箇所、未接続UIコールバック、バウンス／プラグイン／GPUのスタブは
[DEEP_IMPLEMENTATION_GAP_AUDIT.md](docs/DEEP_IMPLEMENTATION_GAP_AUDIT.md) に集約する。
- Missing音源判定：実装済み
- ワークスペーステスト：20件全件パス

### 残りの必須作業

1. macOS実機でアプリを起動する
2. 実際のオーディオデバイスから音が出ることを確認する
3. WAVインポート、波形表示、再生を確認する
4. Save As → 再起動 → Openを確認する
5. Missing音源の再リンクとショートカットを確認する
6. デバイス切断・再接続・自動Pauseを確認する
7. 30分連続再生と100トラック程度の負荷を確認する

### 期間の目安

環境依存の問題がなければ、コード実装はほぼプレビュー候補段階です。

- 最初の実ビルド・起動確認：半日〜1日
- 音声入出力と基本操作の修正：1〜3日
- 長時間・高負荷・切断復旧テスト：1〜2日
- 配布パッケージと最終修正：1〜2日

したがって、実機環境が整っていれば、**数日程度で内部プレビュー版**を作れる見込みです。一般配布向けの品質確認まで含める場合は、実機テスト結果次第で1〜2週間程度を見込むのが安全です。

## 最初の実ビルド計画

### 1. 開発ビルド

```bash
cargo build --workspace
```

### 2. テスト付きビルド確認

```bash
cargo fmt --all -- --check
cargo check --workspace
cargo test --workspace
```

### 3. 起動時にプロジェクトを開く

```bash
cargo run -p aura-ui -- /path/to/project.aura
```

### 4. 実機スモークテスト

- [ ] アプリが起動する
- [ ] `AUDIO: READY` が表示される
- [ ] `SR: 44100 Hz` または実デバイスのサンプルレートが表示される
- [ ] Open / Import / Saveが動作する
- [ ] 実音声が再生される
- [ ] 波形が表示される
- [ ] デバイス切断でPauseされる

### 5. 保守性レビューで確認すること

- [ ] ビルド警告が新たに増えていない
- [ ] C++とRustのFFI宣言が一致している
- [ ] UIに固定値・ダミー値が残っていない
- [ ] 実機依存コードがmacOS以外のビルドを壊していない
- [ ] 保存・読み込み失敗時に既存状態が壊れない
- [ ] オーディオコールバック内にI/Oや待機ロックがない
- [ ] ビルド成果物をクリーン環境で再現できる

## 最新実装反映（更新）

### 追加反映

- [x] Missing音源のCore再リンクAPI
- [x] `RELINK MISSING` コマンド
- [x] `⌘⇧I / Ctrl+Shift+I` による再リンク操作
- [x] 再リンク後の音声データ・長さ・波形の再同期
- [x] 再リンク失敗時の安全なエラー表示

### 新たに実装済み

- [x] Open Project用のネイティブファイル選択ダイアログ
- [x] Audio Import用のネイティブファイル選択ダイアログ
- [x] `.aura` / `.json`プロジェクトフィルター
- [x] WAV / AIFF / FLAC音声フィルター
- [x] ファイル選択後のCore読み込み・UI再同期
- [x] CLI第1引数による起動時プロジェクトOpen
- [x] CoreAudioデバイス稼働状態のFFI公開
- [x] UIへの `AUDIO: READY / OFFLINE` 表示
- [x] デバイス切断時の自動Pause
- [x] デバイス切断時の再生ヘッド更新停止
- [x] UIの選択行番号とCoreトラックIDの分離
- [x] Audio Import時の実トラックID解決
- [x] Vocal Separation時の実トラックID解決
- [x] 失敗時の `NO TRACK` / `IMPORT FAILED` 表示

### 次に実機で確認する項目

- [ ] `OPEN PROJECT`から実際のプロジェクトを選択する
- [ ] `IMPORT AUDIO`から実際のWAVを選択する
- [ ] 選択した音声が正しいトラックへ入る
- [ ] トラック削除後に別トラックへ誤インポートされない
- [ ] トラック並び替え後に誤ったトラックへ処理されない
- [ ] Audio DeviceがREADYと表示される
- [ ] ヘッドフォン・USBデバイス切断時にOFFLINE表示になる
- [ ] 切断中に再生がPauseされる
- [ ] 再接続後にユーザー操作で再生できる
- [ ] CLI引数でOpenしたプロジェクトがUIに復元される

### 現時点の実機検証課題

- [ ] デバイス再接続の実機検証（コード上は定期的な再接続試行を実装済み）
- [ ] Open/Importのファイル選択と読み込みを実機で確認する
- [ ] Missing音源の再リンクを実機で確認する
- [ ] Save Asと再起動後のOpen・復元を実機で確認する
- [ ] SR表示をmacOS実機で確認する
- [ ] macOS署名・配布パッケージを実機で確認する
- [ ] 30分連続再生の実機確認
- [ ] 100トラック規模の負荷確認

### 残課題の明確化

- [ ] macOS実機でOpen／Save As／Import、ショートカット、デバイス切断・再接続、自動Pauseを検証する
- [ ] Missing音源を実機で再リンクし、移動・削除された音源を復旧できることを検証する

## 実機スモークテスト手順

```text
1. オーディオデバイスを接続する
2. アプリを起動する
3. AUDIO: READYを確認する
4. Command Spotlightを開く
5. OPEN PROJECTを実行してプロジェクトを選択する
6. トラック・リージョン・波形を確認する
7. IMPORT AUDIOを実行してWAVを選択する
8. 選択中トラックに波形が追加されたことを確認する
9. 再生・停止・Pauseを確認する
10. 再生中にオーディオデバイスを切断する
11. AUDIO: OFFLINEと自動Pauseを確認する
12. デバイスを再接続し、AUDIO: READYとSR表示を確認して手動で再生する
13. プロジェクトを保存し、同じSave pathへ再度保存できることを確認する
14. アプリを再起動し、CLI引数またはOPEN PROJECTで復元する
15. 音源ファイルを移動または削除し、Missing状態の扱いを確認する
```
